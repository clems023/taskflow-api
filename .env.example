# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=taskflow
DB_USER=taskflow
DB_PASSWORD=

# JWT — clé Base64 d'au moins 256 bits
# Générer avec : openssl rand -base64 32
JWT_SECRET=

# Server
SERVER_PORT=8080
JWT_EXPIRATION_MS=86400000
